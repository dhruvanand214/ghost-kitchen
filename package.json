{
  "name": "ghost-kitchen",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "dependencies": {
    "graphql": "^16.12.0"
  }
}
